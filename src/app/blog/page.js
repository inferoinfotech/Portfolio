import React from 'react'

export const metadata = {
    title: 'Blog',
    description: 'This is the blog page',
}

const Blog = () => {
    return (
        <div>Blog</div>
    )
}

export default Blog
