import React from 'react'

export const metadata = {
    title: "Work",
    description: "Explore our work and projects.",
};

const Work = () => {
    return (
        <div>Work</div>
    )
}

export default Work

